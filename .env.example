# Server Configuration
PORT=5000

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production
JWT_TOKEN_DURATION=604800  # 7 days in seconds

# Database Configuration
DB_TYPE=sqlite  # sqlite or postgres
DB_HOST=localhost
DB_PORT=5432
DB_USER=expense_user
DB_PASSWORD=expense_password
DB_NAME=expense_tracker.db  # For sqlite: filename, for postgres: database name
DB_SSLMODE=disable